// Toast timing (ms)
export const TOAST_REMOVE_DELAY_MS = 5_000;

// Delay before focusing the first form field after scrolling to it (ms)
export const SCROLL_FOCUS_DELAY_MS = 600;

// Form field character limits
export const NAME_MAX = 100;
export const EMAIL_MAX = 255;
export const ADDRESS_MAX = 300;
export const PHONE_MAX = 30;
export const DESCRIPTION_MAX = 1_000;

// Anchor IDs used in multiple components
export const ANCHOR_STRATEGY_CALL = "strategy-call";
export const ANCHOR_PROPERTY_OPTIONS = "property-options";
export const ANCHOR_HERO_FORM = "hero-form";
